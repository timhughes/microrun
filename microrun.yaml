
services:
  kafka:
    command:
    - ping
    - 127.0.0.1
    workingdir: /tmp
    displayname: Kafka
    environment: {}
  mongo:
    command:
    - 'sleep 70'
    workingdir: '.'
    displayname: Mongo Database
    environment: {}
  zookeeper:
    command:
    - 'less /var/log/datadog/agent.log'
    workingdir: /var/lib/
    displayname: Zookeeper
    environment: {}
